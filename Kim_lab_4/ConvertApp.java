public class ConvertApp extends ConvertFrame{
    public static void main(String[] args)
    {
        ConvertFrame convertFrame = new ConvertFrame();
        convertFrame.setDefaultCloseOperation(EXIT_ON_CLOSE);
        convertFrame.setSize(350, 450); // set frame size
        convertFrame.setVisible(true); // display frame
    }
}
